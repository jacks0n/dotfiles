# Mask as Firefox on Linux, some sites don't like curl
user-agent = "Mozilla/5.0 (X11; Linux x86_64; rv:28.0) Gecko/20100101 Firefox/36.0"

# When following a redirect, automatically set the previous URL as referer
referer = ";auto"

